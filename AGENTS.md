# 前端仓库协作规则

本文件适用于 `yudao-ui-admin-vue3` 前端仓库全目录。独立克隆本仓库时也必须遵守以下规则，不依赖后端仓库中的本地文件。

## 零、工作区与仓库关系

- 当前双仓库工作区为 `E:\YUDAO\CRM`。
- 当前前端仓库目录为 `E:\YUDAO\CRM\yudao-ui-admin-vue3`，GitHub 唯一仓库为 `https://github.com/LauiXie/crmonevue.git`。
- 配套后端仓库目录为 `E:\YUDAO\CRM\ruoyi-vue-pro`，GitHub 唯一仓库为 `https://github.com/LauiXie/crmonejava.git`。
- 前后端是两个独立 Git 仓库。所有 Git 命令、状态检查、暂存和提交都必须在当前目标仓库根目录执行，不能跨仓库暂存文件。
- 前端负责 Vue 页面、API 调用、动态路由、静态资源和前端构建配置；后端负责 Java 服务、数据库脚本、部署编排以及统一的项目级文档。
- 双仓库同时存在时，接口、菜单权限和部署变化要核对两边影响。独立克隆前端时，不得假设上述后端本地目录存在；需要后端配合或更新统一文档时，应记录后端仓库 URL、接口或文档目标及待同步内容。

## 一、通用规则

- 默认使用中文沟通；只有用户明确要求时才改用其他语言。
- 开始工作前完整读取当前仓库的 `AGENTS.md`，再核对任务范围。
- 修改前检查 `git status`，修改后检查 `git diff`，并逐一核对新增文件和删除列表。
- 保留用户现有未提交改动，禁止 `git reset`、`git checkout` 或覆盖用户改动。
- 采用小范围、小步修改，不进行与任务无关的组件重写、依赖升级、批量格式化或大重构。
- 不得提交密码、Token、访问密钥、真实后端地址、生产 `.env` 或其他生产环境配置；环境变量示例必须使用安全占位值。
- 未经用户明确授权，不得提交、推送、删除文件、切换或创建分支、修改远程仓库。
- 路径、组件引用和导入语句必须兼容 Debian 13 的大小写敏感规则，文件名大小写应与 import 完全一致。

## 二、版本管理

- 本仓库只使用 `main` 分支，不创建 `develop`、`feat/*`、`fix/*` 或其他分支。若当前分支不符合目标规范，只在用户明确授权的仓库初始化或发布任务中调整，并且不得改写提交历史。
- 配套后端唯一仓库为 `https://github.com/LauiXie/crmonejava.git`；不得把后端代码或发布指向其他仓库。
- 前端唯一仓库为 `https://github.com/LauiXie/crmonevue.git`，应作为唯一 `origin`。
- 不保留原项目 `upstream`，不向原项目推送，也不从原项目拉取或合并代码。
- 严禁 force push，包括 `--force-with-lease`。
- 用户授权提交后，按页面、路由、API 或品牌项拆分为可审查的小步提交；与后端配套发布时使用相同 Git Tag。
- 修改远程地址、推送和创建 Tag 前都必须再次确认已获得当前任务授权。

## 三、项目范围

- 保留前端框架、登录与个人中心、必要的 `system`/`infra` 管理能力、CRM 页面与 API。
- BPM 只保留 CRM 合同和回款所需的最小审批能力，包括待办、已办、流程实例详情和审批操作；对用户统一呈现为 CRM 审批支撑能力。
- 商城、IoT、ERP、AI、IM、MES、WMS、会员、支付、公众号和报表等无关页面、API、状态和专属组件最终应实际删除，不以大面积注释作为完成状态。
- 删除公共组件、状态或依赖前必须扫描引用；无法确认是否被 CRM、`system`、`infra` 或 BPM 共用时先保留并报告。
- 第一阶段不配合后端全面重命名 Java 包名；前端只调整对外可见品牌和已确认的接口、路由及构建配置。
- 浏览器标题、Logo、favicon、登录页、首页、页脚、外链、导出文件名、数据库展示数据、构建产物和运行环境均需清理原项目品牌。

## 四、数据库规则

- 数据库原始基线位于后端仓库 `sql/mysql/ruoyi-vue-pro-初始表结构20260302.sql`；它不能直接作为部署脚本。
- 数据库交付物必须由该基线生成精简、脱敏、可重复初始化的 CRM SQL，并保留 CRM、必要 `system`/`infra`、BPM 和 Flowable 相关表。
- 目标范围外的业务表、菜单、权限、字典和样例数据必须移除；前端路由、权限标识和菜单组件名要与精简后的菜单数据一致。
- 前端仓库不复制或另行维护数据库脚本；相关项目级文档和 SQL 统一由后端仓库管理。
- 发现接口响应、环境文件或文档中存在疑似凭据时，不得输出完整值，也不得提交或推送；仅以脱敏形式报告位置和风险。

## 五、文档规则

- 项目级开发、数据库、部署、运维和业务文档统一维护在后端仓库 `docs/`，主指导文档为 `docs/CRM演示系统开发与部署指导-初稿.md`。
- 前端功能范围、路由架构、构建部署方式或品牌清理范围变化时，必须在配套后端工作区同步更新指导文档；无法同时访问后端仓库时，应明确列出待同步内容。
- 本 `AGENTS.md` 必须保留前端独立开发所需的完整规则，不得改成只指向后端本地路径的简略说明。
- 文档中的 pnpm 命令、环境名称、路径和组件名应以当前仓库实际配置为准。

## 六、验证规则

- 前端源码、页面、API、路由、组件、依赖或构建配置发生变化后，至少执行：

  ```bash
  pnpm ts:check
  pnpm build:prod
  ```

- 删除页面或模块后，必须扫描动态路由、菜单组件名、`import.meta.glob` 懒加载引用、API 引用和共享组件引用，并检查生产构建产物中无残留入口。
- 配套任务包含后端改动时，后端至少执行与改动范围匹配的 Maven 编译或测试；修改模块加载后还要核对根聚合与启动器依赖，并验证聚合构建和应用启动。
- 涉及 CRM 审批的改动应回归合同和回款跳转、流程实例详情及审批操作；涉及权限的改动应使用普通角色验证不可见与不可访问状态。
- 纯文档改动可不执行前端构建，但必须核对文档内容、路径及 `git diff`，并在交付时说明未运行命令的原因。
